# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
.venv/
venv/
env/
ENV/

# IDEs
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Jupyter
.ipynb_checkpoints/
*.ipynb_meta

# Testing & coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/
.ruff_cache/

# Data — keep structure, ignore contents (except small reference files <100kB)
data/raw/
data/processed/
data/cache/
*.parquet
*.h5
*.pkl
# External datasets (TEP for Phase 4 cross-domain anomaly training, etc.)
# — bulk binaries ignored; provenance docs and small metadata kept.
data/external/**/*.csv
data/external/**/*.dat
data/external/**/*.tsv
data/external/**/*.mat
data/external/**/*.RData
data/external/**/*.zip
data/external/**/*.tar
data/external/**/*.tar.gz
data/external/**/*.json
!data/external/**/file_listing.json
!data/external/**/provenance.json
# Whitelist sweep-cache state files used by lookup_lv_ss for off-nominal
# robustness checks and Phase-3 MPC linearizations — these are versioned
# reference artifacts, not run outputs.
!data/reference/*.parquet

# Outputs / artifacts
runs/
results/
*.png
!docs/**/*.png
!paper/figures/*.png

# Secrets
.env
.env.local
*.key

# OS
Thumbs.db

# Pyomo / IDAES temporary files
*.nl
*.sol
tmp_solve/
